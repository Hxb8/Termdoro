# Rust build output (never commit this)
target/

# Go build output (root-anchored so cmd/termdoro/ is not ignored)
/termdoro
/termdoro.exe
dist/
*.tar.gz
*.zip

# Go test/coverage artifacts
*.test
*.out
coverage.html
